services:
  api:
    build: .
    container_name: api-almacen-ropa
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./app.db
      SECRET_KEY: change-this-secret-key-in-production
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
    volumes:
      - ./:/app
